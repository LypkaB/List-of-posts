import React from 'react';
import './search-panel.scss';

const SearchPanel = () => {
    return (
        <input className="form-control search-input" type="text" placeholder="Posts search" />
    )
};

export default SearchPanel;
